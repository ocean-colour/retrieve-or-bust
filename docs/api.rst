API reference
=============

.. note::

   Stub. The single ``automodule`` page (IOPtics' pattern, DocQ4) is written
   at D1 task 5, with one block per module in the order ``robust.rt``'s own
   ``__init__`` docstring uses: ``conventions``, ``types``, ``data.l23``,
   ``ed``, ``ztt``, ``emulator``, ``hybrid``, ``inelastic``,
   ``inelastic_corr``, ``baselines``, ``validation``.

   Autodoc imports the real package -- ``autodoc_mock_imports`` is empty by
   design (DocQ2) -- so the signatures and ``jaxtyping`` shape annotations
   rendered here will be the real ones.

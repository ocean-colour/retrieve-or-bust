# Downwelling irradiance

`Ed(θ_s, λ)` at the surface from the three packaged L23 sky spectra, the zenith
interpolation, and the `Geometry.Ed` override where a real sky plugs in.

:::{note}
Arrives at D2 — until then, {doc}`overview` is the one-page summary of this piece.
:::

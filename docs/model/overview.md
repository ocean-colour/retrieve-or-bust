# The model in one page

```{note}
Stub. Written at D1 task 6: the composition law, what each term is and which
module owns it, the three `mode` values, the `inelastic=None` bit-identity
guarantee, and a concept → module → API-anchor table. The per-chapter pages
that hang off this one arrive at D2.
```

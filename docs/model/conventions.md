# Conventions

The shared choices every other page assumes: `Rrs` ↔ `rrs` (A = 0.52, B = 1.7),
the 81-point canonical wavelength grid and `check_wave`, and pure-water
backscattering `bb_w(λ)`.

:::{note}
Arrives at D2 — until then, {doc}`overview` is the one-page summary of this piece.
:::

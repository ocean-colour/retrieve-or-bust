# The learned residual

The small Flax MLP that predicts the dimensionless residual the backbone misses,
its training data and domain guard, and why `mode='emulator'` is a term rather
than a model.

:::{note}
Arrives at D2 — until then, {doc}`overview` is the one-page summary of this piece.
:::

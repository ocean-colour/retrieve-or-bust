# `forward()` and the composition law

The public entry point in detail: the `mode` flag, the domain guard and
`on_out_of_domain`, and why additivity holds in `rrs` space but not in `Rrs`
space.

:::{note}
Arrives at D2 — until then, {doc}`overview` is the one-page summary of this piece.
:::

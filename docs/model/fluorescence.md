# Chlorophyll-a fluorescence

The φ_C-linear emission kernel, the excitation quadrature, `emission_line`, and
the `'single'` versus `'double'` emission shapes.

:::{note}
Arrives at D2 — until then, {doc}`overview` is the one-page summary of this piece.
:::

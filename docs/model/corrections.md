# The correction heads

The two learned, bounded heads that rescale the analytic inelastic terms:
`HeadConfig`, the packaged weights, `corrections=None` versus `False`, and the
tanh bounds.

:::{note}
Arrives at D2 — until then, {doc}`overview` is the one-page summary of this piece.
:::

# Validation

The acceptance protocol in `robust/rt/validation.py`: rRMS in `rrs` space and
its breakdowns, the inelastic increment metrics, the speed ratio, the gradient
reports, and the measured results against the design's acceptance gate.

:::{note}
Arrives at D2 — until then, the front page's *What exists, and what does not*
section carries the headline numbers and where they were measured.
:::

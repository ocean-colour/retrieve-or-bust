# Quickstart

```{note}
Stub. This page is written at D1 task 4: build `IOPs` / `PhaseParams` /
`Geometry` for one L23 scene, call `forward()`, switch the inelastic terms on,
and take a gradient — with every printed number pasted from a real run. An
executed notebook version follows at D2.
```
